@model ProdutoModel

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="page-container">
    <div class="form-card">

        <h1>Editar Produto</h1>

        <form method="post"
              asp-controller="Produto"
              asp-action="Inserir">

            <div class="form-group">
                <label>Categoria</label>
                <select name="produto.IdCategoria">
                    <option selected value="0"></option>
                    @foreach (var categoria in ViewBag.categorias)
                    {
                        if (categoria.Id == Model.IdCategoria)
                        {
                            <option selected value="@categoria.Id">@categoria.Nome</option>
                        }
                        else
                        {
                            <option value="@categoria.Id">@categoria.Nome</option>
                        }
                    }
                </select>
            </div>

            <input type="hidden"
                   name="produto.Id"
                   value="@Model.Id" />

            <input type="hidden"
                   name="produto.Disponivel"
                   value="@Model.Disponivel" />

            <input type="hidden"
                   name="produto.Excluido"
                   value="@Model.Excluido" />

            <div class="form-group">
                <label>Nome</label>
                <input type="text"
                       name="produto.Nome"
                       value="@Model.Nome" />
            </div>

            <div class="form-group">
                <label>Quantidade em Estoque</label>
                <input type="number"
                       min="1"
                       name="produto.QuantidadeEstoque"
                       value="@Model.QuantidadeEstoque" />
            </div>

            <div class="form-group">
                <label>Preço</label>
                <input type="number"
                       min="1"
                       step="0.01"
                       name="produto.Preco"
                       value="@Model.Preco" />
            </div>

            <div class="form-group">
                <label>Estoque Mínimo para Venda</label>
                <input type="number"
                       min="1"
                       name="produto.EstoqueMinimoVenda"
                       value="@Model.EstoqueMinimoVenda" />
            </div>

            <div class="form-group">
                <label>Código</label>
                <input type="text"
                       name="produto.Codigo"
                       value="@Model.Codigo" />
            </div>

            <div class="form-group">
                <label>Descrição</label>
                <textarea name="produto.Descricao"
                          rows="4">@Model.Descricao</textarea>
            </div>

            <div class="action-buttons">

                <button type="submit" class="btn-salvar">
                    Salvar
                </button>

                <a class="btn-excluir"
                   asp-controller="Produto"
                   asp-action="Delete"
                   asp-route-id="@Model.Id">
                    Excluir
                </a>

                <a class="btn-cancelar"
                   asp-controller="Produto"
                   asp-action="Index">
                    Cancelar
                </a>

            </div>

        </form>

    </div>
</div>

<style>
    body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
        background: #f4f6f9;
    }

    .page-container {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px;
    }

    .form-card {
        width: 100%;
        max-width: 550px;
        background: white;
        padding: 35px;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,.15);
    }

        .form-card h1 {
            text-align: center;
            margin-bottom: 25px;
            color: #333;
        }

    .form-group {
        display: flex;
        flex-direction: column;
        margin-bottom: 18px;
    }

        .form-group label {
            margin-bottom: 6px;
            font-weight: bold;
            color: #555;
        }

        .form-group input,
        .form-group textarea {
            padding: 12px;
            border: 1px solid #dcdcdc;
            border-radius: 6px;
            font-size: 14px;
            transition: .3s;
            resize: vertical;
        }

            .form-group input:focus,
            .form-group textarea:focus {
                outline: none;
                border-color: #0078d7;
                box-shadow: 0 0 5px rgba(0,120,215,.3);
            }

    .action-buttons {
        display: flex;
        gap: 10px;
        margin-top: 25px;
        flex-wrap: wrap;
    }

    .btn-salvar,
    .btn-excluir,
    .btn-cancelar {
        flex: 1;
        padding: 12px;
        border-radius: 6px;
        text-align: center;
        text-decoration: none;
        font-weight: bold;
        cursor: pointer;
        transition: .3s;
    }

    .btn-salvar {
        background: #28a745;
        color: white;
        border: none;
    }

        .btn-salvar:hover {
            background: #218838;
        }

    .btn-excluir {
        background: #dc3545;
        color: white;
    }

        .btn-excluir:hover {
            background: #c82333;
        }

    .btn-cancelar {
        background: #6c757d;
        color: white;
    }

        .btn-cancelar:hover {
            background: #5a6268;
        }

    .form-group select {
        padding: 12px;
        border: 1px solid #dcdcdc;
        border-radius: 6px;
        font-size: 14px;
        transition: .3s;
        resize: vertical;
        width: 100%;
        background: #fff;
    }

        .form-group select:focus {
            outline: none;
            border-color: #0078d7;
            box-shadow: 0 0 5px rgba(0,120,215,.3);
        }
</style>